import { GET as aiGet, POST as aiPost } from '@/app/api/ai/report-analysis/route';

export const dynamic = 'force-dynamic';
export const runtime = 'nodejs';

export const GET = aiGet;
export const POST = aiPost;
